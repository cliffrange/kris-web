import styled from "styled-components";
import { TableCell } from "grommet";

export const Type = styled(TableCell)`
  font-size: 15px;
`;

export const Text = styled(TableCell)`
  text-align: right;
  font-size: 15px;
`;
