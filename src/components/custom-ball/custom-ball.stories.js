import React from "react";
import { CustomBall } from "./custom-ball";

export default { title: "Custom Ball" };

export const normal = () => {
  return (
    <div>
      <CustomBall/>
    </div>
  );
};